<h3>Drop a Table</h3>

Drop a table is one shot function simply to use, it check IF EXISTS the table name to not drop table that not exists.

<pre>
$myPulse->dropTable('table_name');
</pre>

There's no words to explain so simple concept.